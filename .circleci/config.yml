version: 2.1

orbs:
 android: circleci/android@0.2.0

jobs:
  build:
    executor: android/android

    environment:
      JVM_OPTS: -Xmx3g
        
    steps:
      - checkout
      - run:
          command: ./gradlew check
